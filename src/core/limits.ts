export const LIMITS = {
  maxSourceBytes: 256 * 1024 * 1024,
  maxZipEntries: 4096,
  maxExpandedEntryBytes: 128 * 1024 * 1024,
  maxExpandedZipBytes: 512 * 1024 * 1024,
  maxCompressionRatio: 200,
  maxEncodedChunkBytes: 64 * 1024 * 1024,
  maxInflatedChunkBytes: 256 * 1024 * 1024,
  maxInflatedCanvasBytes: 384 * 1024 * 1024,
  maxNodes: 250_000,
  maxParentDepth: 256,
  maxBlobBytes: 16 * 1024 * 1024,
  maxAllBlobBytes: 256 * 1024 * 1024,
  maxDecodedStringUnits: 1 * 1024 * 1024,
  maxVectorVertices: 100_000,
  maxVectorSegments: 100_000,
  maxVectorRegions: 100_000,
  maxVectorLoops: 100_000,
  maxVectorIndices: 250_000,
  maxVectorAllocations: 500_000,
  defaultContextDepth: 2,
  maxContextDepth: 8,
  defaultContextNodes: 64,
  maxContextNodes: 256,
  defaultTextUnitsPerNode: 1024,
  maxTextUnitsPerNode: 4096,
  defaultTextUnitsPerResponse: 16_384,
  maxTextUnitsPerResponse: 32_768,
  defaultAssets: 32,
  maxAssets: 64,
  defaultGuidance: 16,
  maxGuidance: 32,
  defaultAncestry: 32,
  maxAncestry: 64,
  defaultScreens: 100,
  maxScreens: 500,
  maxSerializedResponseBytes: 128 * 1024,
} as const;
export type ResourceLimit = keyof typeof LIMITS;
export function assertLimit(name: ResourceLimit, observed: number, message = `${name} exceeded`) { const limit = LIMITS[name]; if (observed > limit) throw new Error(`${message}: observed ${observed}, limit ${limit}`); }
